services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY:-}
    volumes:
      - db-data:/app/app
    restart: unless-stopped

volumes:
  db-data:
